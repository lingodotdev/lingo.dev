name: Claude Code PR Review

on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  issues: read

jobs:
  claude-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Claude Code
        run: |
          npm install -g @anthropic-ai/claude-code
          claude --version

      - name: Get PR diff
        id: pr-diff
        run: |
          # Get the diff for this PR
          git fetch origin main
          git diff origin/main...HEAD > pr_diff.txt
          
          # Check if diff is not empty
          if [ ! -s pr_diff.txt ]; then
            echo "No changes to review"
            exit 0
          fi
          
          echo "diff_exists=true" >> $GITHUB_OUTPUT

      - name: Review PR with Claude Code
        if: steps.pr-diff.outputs.diff_exists == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.CLAUDE_CODE_ANTHROPIC_API_KEY }}
        run: |
          # Create a comprehensive review prompt
          cat > review_prompt.txt << 'EOF'
          Please review this pull request diff for a TypeScript/JavaScript monorepo project. Focus on:

          1. **Code Quality**: Check for best practices, maintainability, and adherence to existing patterns
          2. **Security**: Look for potential security vulnerabilities or sensitive data exposure
          3. **Performance**: Identify potential performance issues or inefficiencies
          4. **Testing**: Ensure adequate test coverage for new functionality
          5. **Documentation**: Check if changes need documentation updates
          6. **Breaking Changes**: Identify any potential breaking changes
          7. **Dependencies**: Review new dependencies and their necessity
          8. **Monorepo Considerations**: Ensure changes respect monorepo structure and dependencies

          For each issue found, provide:
          - File and line reference
          - Clear description of the issue
          - Suggested fix or improvement
          - Severity level (critical, high, medium, low)

          If no significant issues are found, provide a brief positive summary.

          Here's the PR diff:
          EOF
          
          # Append the diff to the prompt
          cat pr_diff.txt >> review_prompt.txt
          
          # Run Claude Code review
          claude -p --output-format json --max-turns 1 < review_prompt.txt > claude_review.json
          
          # Extract the review content
          cat claude_review.json | jq -r '.content' > claude_review.md

      - name: Post review comment
        if: steps.pr-diff.outputs.diff_exists == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            try {
              const reviewContent = fs.readFileSync('claude_review.md', 'utf8');
              
              // Create comment body with proper formatting
              const commentBody = `## 🤖 Claude Code Review
              
              ${reviewContent}
              
              ---
              *This review was generated by Claude Code. Please use your judgment when addressing the suggestions.*`;
              
              // Post the comment
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: commentBody
              });
              
              console.log('Review comment posted successfully');
            } catch (error) {
              console.error('Error posting review:', error);
              // Don't fail the workflow if comment posting fails
            }

      - name: Check for critical issues
        if: steps.pr-diff.outputs.diff_exists == 'true'
        run: |
          # Check if Claude identified any critical issues
          if grep -qi "critical\|security\|vulnerability" claude_review.md; then
            echo "⚠️ Critical issues found in PR. Please review carefully."
            echo "critical_issues=true" >> $GITHUB_OUTPUT
          else
            echo "No critical issues identified."
            echo "critical_issues=false" >> $GITHUB_OUTPUT
          fi
        id: critical-check

      - name: Summary
        if: steps.pr-diff.outputs.diff_exists == 'true'
        run: |
          echo "Claude Code review completed for PR #${{ github.event.number }}"
          if [ "${{ steps.critical-check.outputs.critical_issues }}" = "true" ]; then
            echo "⚠️ Critical issues were identified - please review the comments"
          else
            echo "✅ Review completed - check the PR comments for feedback"
          fi