---
"lingo.dev": patch
---

# Changelog

## 2.0.0

### Added

- 9 layout options (`center`, `top`, `bottom`, `left`, `right`, `top-left`, `top-right`, `bottom-left`, `bottom-right`, `split`)
- Multi-language support via Lingo.dev
- Subtitle support
- Light and dark themes
- Custom background and text colors
- Custom dimensions (200–4000px)
- Custom font size and weight
- Adjustable padding
- Built-in caching
- Rate limiting (100 req/min per IP)
- Compression (gzip)
- CORS support
- Health endpoint (`/health`)
- Documentation endpoint (`/api/docs`)
- Layout listing endpoint (`/api/layouts`)
- Full input validation and structured error handling

### Improved

- Optimized SVG generation
- Better text wrapping for long content
- Translation fallback handling
- Enhanced error responses
