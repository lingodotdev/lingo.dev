import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'LingoLive - Real-Time Multilingual App Preview',
  description: 'Experience how Lingo.dev transforms your content into multiple languages instantly. See your UI text, error messages, and dynamic content localize in real-time.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}