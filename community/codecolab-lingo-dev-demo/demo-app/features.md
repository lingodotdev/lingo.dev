# Demo Application Features

## 1. Runtime Language Switching
Users can switch languages from the UI.
All visible interface text updates immediately.

## 2. Localized Authentication
Login and signup flows are fully localized,
including error messages.

## 3. Editor UI Localization
Non-code UI elements (buttons, status labels)
are localized without touching editor content.
