# ⏳ ChronoLingo

**ChronoLingo** is a Python Flask application that demonstrates how to integrate **Lingo.dev** into a dynamic web environment.

## 🚀 What it does
It wraps the Lingo.dev CLI in a Python interface, allowing users to input text on a web page, which is then processed by Lingo.dev to generate translations in real-time.

## 🛠️ How to Run
1. **Install Dependencies:**
   ```bash
   pip install -r requirements.txt
   npm install -g lingo.dev